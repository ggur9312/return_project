# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

A static, no-build, no-framework warehouse operations dashboard ("집품·트럭 현황 대시보드") for a single site (`index.html`) with three independent top-level tabs sharing one page, switched via the shared left `#appSidebar`: **대시보드** (반출 대시보드, the default landing tab, no sub-nav of its own), **집품 현황** (picking allocation, its sub-screens — 집품 리스트/집품 할당/집품리스트 추출 — appear as an expandable group under it in `#appSidebar`) and **트럭 현황** (truck/CT cycle management, its sub-screens — 트럭 리스트/트럭주기 — likewise expand under it in `#appSidebar`). There is no server, no bundler, no package manager (no `package.json`), no test suite. Data lives entirely in the browser via `localStorage`; there is no backend API.

## Running it

There is no build/lint/test command — open `index.html` directly by double-clicking it (`file://`). This is the primary/expected way users run the app, so any change must keep working under `file://` — do not reintroduce `<script type="module">` for `js/pick/*.js` (browsers block ES module `import` under `file://`, which is exactly the regression this namespace-object setup fixes). You can also serve it locally if you want, for parity with a real deployment:

```bash
python3 -m http.server 8934
# then open http://localhost:8934/index.html
```

To verify a change actually works, drive the app in a real browser (Playwright works well here — the project has no test framework, so this is the only way to check behavior). A project-specific Playwright recipe (how to seed data via the paste box, screen navigation, gotchas like the O-zone-priority button being inside a collapsed panel) is documented in `.claude/skills/verify/SKILL.md` — read that before writing a fresh verification script.

## Script loading and scope model — read this before editing any JS

`index.html` loads scripts in this exact order, and it matters:

```
js/vendor/tailwindcss.browser.js   (Tailwind, browser JIT build)
js/vendor/JsBarcode.all.min.js
js/vendor/xlsx.full.min.js
js/vendor/chart.umd.js             (Chart.js, UMD build — 대시보드 원형/막대 그래프)
js/truck.js         -- 트럭 현황 screen, NOT wrapped — declares directly in global scope
js/shell.js         -- shared cross-screen UI utils (window.confirmModal / window.showToast / window.flashPageLoading / window.printWithConfirm / window.lockBodyScroll / window.unlockBodyScroll) + owns the left #appSidebar 3-way tab switching (대시보드/집품 현황/트럭 현황), including expanding/collapsing each app's sub-nav group
js/pick/core.js         -- 집품 현황: shared state/utils, must load first
js/pick/gt-print.js     -- 집품 현황: GT barcode/label printing
js/pick/assign-panel.js -- 집품 현황: picking-assignment screen
js/pick/home-table.js   -- 집품 현황: home table rendering/selection (nav label "집품 리스트")
js/pick/extract.js      -- 집품 현황: 집품리스트 추출 screen (2-sheet excel cross-reference)
js/pick/dashboard.js    -- 집품 현황: 홈 대시보드 screen ("반출 대시보드", 독립 A~N 엑셀 업로드 + Chart.js KPI/도넛/막대 그래프)
js/pick/main.js         -- 집품 현황: event wiring + init, must load last
```

- **`js/pick/*.js`** — the 집품 현황 screen, split into 7 files (originally one ~3800-line `js/app.js`; split for editing/reading ergonomics, see next section — a previous 6th file, `custom-assign.js`, was removed once its "create new assignment" screen turned out to fully duplicate home's drag-select-assign flow; `extract.js` and later `dashboard.js` took its place as new domain files for unrelated new features). `dashboard.js`-owned `#dashboardView` (반출 대시보드) used to be 집품현황's own internal "홈" nav item and default sub-view; it has since been promoted to a top-level `#dashboardApp` tab in the shared `#appSidebar` (see "Top-level app switching" below) and removed from 집품현황's internal nav entirely — despite the promotion, `dashboard.js` itself needed **no changes**, since its rendering code only queries elements by id and never depended on DOM nesting position. The original home screen (upload/paste/table/필터/집품할당 카드, still owned by `core.js`/`home-table.js`/`main.js`, DOM id still `#homeView`) was relabeled "집품리스트 현황" in the sidebar without changing its internal id/logic, and is now 집품현황's own default sub-view (nav's "홈" slot is gone, `#navHomeBtn`/`#homeView` are default-active instead). **Not ES modules** — each file is a plain classic `<script>`, wrapped in its own IIFE `(function (Pick) { ... })(window.Pick = window.Pick || {})`, and files talk to each other only through the shared `window.Pick` namespace object (`Pick.foo = foo;` to expose, `var foo = Pick.foo;` to consume). This used to be `<script type="module">` with real `import`/`export`, but browsers block ES module `import` under `file://` (CORS), which broke the app for anyone who just double-clicks `index.html` instead of serving it over `http://`. Classic scripts have no such restriction, so the namespace-object pattern was adopted instead — it keeps the same multi-file split (and its token-savings benefit for editing) without requiring a server.
- Load order in `index.html` matters and mirrors the table above: `core.js` first (everyone depends on it), `main.js` last (it wires up events and runs init, which needs every other file's exports already on `Pick`) — placed *after* `js/truck.js`/`js/shell.js` so `window.confirmModal`/`window.showToast` are guaranteed to exist first.
- **`js/truck.js`** is *not* wrapped — its top-level `let`/`const`/`function` declarations are real globals. It is a separate, older subsystem for truck loading cycles; it does not share state with `js/pick/*` except through `window.confirmModal`/`window.showToast` from `shell.js`. Because `js/pick/*.js` are each IIFE-wrapped, their internals never leak into `truck.js`'s global scope — only `window.Pick` itself is shared, so double-check any new top-level name in `js/pick/*.js` doesn't collide with something `truck.js` already declares globally (there is no automated check for this — grep `js/truck.js` for the name).
- **`js/shell.js`** provides tiny cross-cutting UI helpers (loading overlay, a shared confirm-modal promise wrapper, toast notifications, `window.printWithConfirm` — the double-rAF-then-`afterprint`-then-confirm-modal wrapper used by all 7 print flows across both apps, and `window.lockBodyScroll`/`window.unlockBodyScroll` — a reference-counted pair that every modal open/close routine in the app calls, so the background page can't scroll while a modal is open even if modals stack) — no domain logic — **and** owns the left `#appSidebar` 3-way tab switching (see below). It's the only file that knows about `#dashboardApp`/`#pickApp`/`#truckApp` as siblings.
- `js/vendor/*` are unmodified third-party libraries (Tailwind JIT ~270KB, JsBarcode ~60KB, xlsx ~880KB). **Never grep or read these** — they are not part of this app's logic and reading them wastes a large amount of context for no benefit.

## Top-level app switching (`#appSidebar`)

The shared left sidebar (`#appSidebar`, `bg-slate-800`, sits beside — not above — all three app containers inside a top-level `<div class="flex min-h-screen">` wrapper) has 3 full-width top-level buttons — `#switchToDashboardBtn`/`#switchToPickBtn`/`#switchToTruckBtn` — plus an unrelated `#calcOpenBtn` (calculator widget) pinned to the bottom via `mt-auto`. Click handling, hidden-class toggling of `#dashboardApp`/`#pickApp`/`#truckApp`, and `localStorage["activeAppView"]` persistence all live in a single IIFE in `js/shell.js` (not in `js/pick/main.js` or `js/truck.js`). **대시보드 (`#dashboardApp`) is the default tab on first load** (no saved `activeAppView`) — `#pickApp` and `#truckApp` both start with a static `hidden` class in `index.html`, `#dashboardApp` does not. Returning users with a saved `"pick"`/`"truck"` preference are unaffected. Switching *to* `"dashboard"` specifically also calls `window.Pick.refreshAll()` (guarded, since it's a no-op at the very first parse-time `apply()` call before `js/pick/main.js` has run) — this is required because the dashboard's zone/floor bar charts read `state.rows` (집품현황's dataset, which can change while the dashboard tab is hidden) and `Pick.refreshAll()` skips rendering the dashboard whenever `#dashboardApp` is hidden, so switching *into* it needs an explicit catch-up render. Switching to `"pick"`/`"truck"` does **not** need this, since neither app's data can change while its own container is hidden (all their upload/paste/edit controls live inside the container itself). `#dashboardApp` has no sub-nav of its own — it's just `#dashboardView`'s content directly, mirroring `#pickApp`/`#truckApp`'s "no id-attribute needed beyond hidden-toggling" convention.

**Expandable sub-nav groups.** 집품현황과 트럭현황이 각자 따로 갖고 있던 내부 sidebar(`#mainNavAside`와 트럭현황의 무명 `<aside>`)는 제거되었고, 그 버튼들은 `#appSidebar` 안, 해당 앱 버튼 바로 아래에 있는 `#pickSidebarSubnav`(집품리스트 현황/집품 할당/집품리스트 추출 3개 버튼)와 `#truckSidebarSubnav`(트럭리스트 현황/트럭주기 2개 버튼) 컨테이너로 옮겨졌다. "집품현황 클릭 → 그 아래 서브 항목이 펼쳐짐"은 새로운 확장/축소 상태가 아니라 기존 `activeAppView` 전환의 부수 효과다. 서브 항목 버튼(`#navHomeBtn`/`#navAssignBtn`/`#navExtractBtn`, `#truckNavHomeBtn`/`#navCycleBtn`) 자체는 id/클릭 배선 그대로이며, `switchView()`(core.js)/`truckSwitchView()`(truck.js)는 완전히 DOM 위치 독립적이라 이 이동에 로직 변경이 필요 없었다.

**펼침 애니메이션 (grid-template-rows 트릭).** 처음엔 `classList.toggle("hidden", ...)`로 즉시 표시/숨김이었는데(전환 없이 뚝 끊겨 "다시 렌더링되는" 느낌이 든다는 피드백으로 교체) 지금은 `js/shell.js`의 `apply(view)`가 `pickSidebarSubnav`/`truckSidebarSubnav`의 `className` 전체를 `grid-rows-[0fr]`(접힘)⇄`grid-rows-[1fr]`(펼침) 사이로 교체하며 `transition-[grid-template-rows]`로 부드럽게 애니메이션한다. 이 트릭이 실제로 0으로 접히려면 **바깥 grid 컨테이너 자체가 아니라, 그 직계 자식(grid item)에 `overflow-hidden`과 `min-h-0`를 반드시 둬야 한다** — grid item의 기본 `min-height: auto`가 콘텐츠 높이를 강제해서 `0fr` 트랙이 실제로는 0으로 안 줄어드는 게 이 기법의 흔한 실패 원인이다(`#pickSidebarSubnav`/`#truckSidebarSubnav` 마크업은 `grid` 바깥 div → `overflow-hidden min-h-0` 중간 div → `flex flex-col gap-1 pl-3 mb-1` 버튼 목록, 3단 구조). 버튼 목록의 `gap-1`/`pl-3`/`mb-1`도 clip되는 중간 레벨이 아니라 가장 안쪽 div에 둬야 한다(margin은 border box 바깥이라 `overflow-hidden`으로도 안 잘려서, 바깥 레벨에 두면 접힌 상태에서 몇 px 새어나온다).

`window.flashPageLoading()`(js/shell.js, 전체 화면을 덮는 로딩 오버레이를 짧게 깜빡이는 순수 시각 연출)은 원래 대분류/중분류 클릭마다(`js/shell.js`의 3개 버튼 핸들러 + `switchView()`(core.js) + `truckSwitchView()`(truck.js), 이 함수의 **유일한** 호출 지점들이었음) 호출됐었는데, 사이드바 탐색이 잦아진 뒤로 "반짝인다"는 피드백을 받아 이 5곳 호출을 전부 제거했다. `flashPageLoading` 함수/`#pageLoadingOverlay` 마크업 자체는 향후 다른 용도로 쓸 수 있게 남겨뒀지만, 현재는 아무도 호출하지 않는다.

**대분류/중분류 활성 스타일 이원화.** 처음엔 대분류(`TOP_ACTIVE`, shell.js)와 중분류(`NAV_BTN_ACTIVE`, core.js/truck.js) 활성 스타일이 셋 다 `"bg-white text-indigo-700 shadow-sm"`로 완전히 동일해서 구분이 안 간다는 피드백에 대분류를 인디고 단색 채움(`TOP_ACTIVE = "bg-indigo-600 text-white shadow-sm"`)으로 먼저 분리했었으나, 중분류의 흰 박스 자체도 "박스가 아니라 글씨만 선택된 느낌이면 좋겠다"는 후속 피드백으로 바뀌었다 — 지금 **중분류 활성(`NAV_BTN_ACTIVE`, core.js/truck.js의 `activeClass`)은 배경/그림자 없이 `text-white`뿐**이다(`bg-white`/`shadow-sm`/`text-indigo-700` 전부 제거, 레이아웃 클래스는 유지). 정적 마크업 기본값도 함께 맞춰야 한다 — `#navHomeBtn`/`#truckNavHomeBtn`은 페이지 첫 로드 시 기본 활성 상태라 `index.html`에 `NAV_BTN_ACTIVE`와 동일한 클래스가 하드코딩돼 있는데, JS가 `switchView()`/`truckSwitchView()`를 통해 실제로 스타일을 갈아끼우는 건 그 버튼이 아닌 다른 뷰로 전환했다가 돌아올 때뿐이므로, `NAV_BTN_ACTIVE` 값을 바꿀 때마다 이 두 정적 클래스도 같이 갱신해야 최초 로드 화면과 클릭 후 화면이 어긋나지 않는다. 비활성 스타일(`TOP_INACTIVE`/`NAV_BTN_INACTIVE`/truck.js의 `inactiveClass`)은 사이드바 배경이 인디고에서 슬레이트로 바뀌면서 셋 다 `"text-slate-300 hover:bg-slate-700/70 hover:text-white"`(고스트 스타일)로 통일돼 있다(변경 없음).

`#switchToDashboardBtn`/`#switchToPickBtn`/`#switchToTruckBtn`(대분류 3개)에는 원래 아이콘이 없었는데(중분류/계산기 버튼엔 이미 있었음) 나중에 추가됐고, 그 뒤 "만들다 만 것 같다"는 피드백으로 몇 개를 개별 교체하는 일이 반복됐다. 그 개별 교체 히스토리는 아래 "아이콘 컨벤션" 전면 통일로 전부 무효화됐으니 현재 상태는 그 항목을 기준으로 볼 것.

## 아이콘 컨벤션 (전부 공식 Heroicons v2.2.0)

앱의 모든 아이콘(index.html 52개 + `js/truck.js`/`js/shell.js`/`js/pick/home-table.js`의 템플릿 문자열 7개 + favicon)은 **공식 Heroicons v2.2.0 원본 path를 그대로** 쓴다. 예전에는 v1을 손으로 고쳐 그린 것(프린터에 종이가 빠지고, 클립보드에 목록 줄이 다르게 그려짐), 아예 직접 그린 것(트럭·계산기·인박스), v1(`stroke-width="2"`)과 v2(`1.5`)가 섞여 있었고 — 이게 "뭉개지고 아마추어가 그린 느낌"의 실제 원인이었다.

**표시 크기에 따라 변형이 갈린다:**

| 표시 크기 | 변형 | `<svg>` 속성 |
|---|---|---|
| `w-4`/`w-5`(≤21px) — 사이드바, 툴바 버튼, 토스트, 닫기 ✕ | **20/solid** | `viewBox="0 0 20 20" fill="currentColor"` (stroke 속성 없음) |
| `w-6`/`w-8`/`w-16`(≥25px) — 서브뷰 헤더 칩, 대시보드 KPI 카드, 드롭존, 빈 상태 | **24/outline** | `fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"` |

루트 폰트 17px 기준 `w-4`는 17px인데 아이콘 그리드는 24라, outline을 쓰면 선 굵기가 `2 × 17/24 = 1.42px`로 픽셀 경계에 안 맞아 흐릿해진다 — **작은 아이콘을 solid로 두는 게 그 흐릿함을 없애는 핵심**이니 `w-4`/`w-5`에 24/outline을 쓰지 말 것. `stroke-width="2"`는 앱 전체에서 사라졌다(0개). 사이드바(solid)와 같은 개념의 헤더 칩(outline)이 다른 변형인 것은 Heroicons 공식 권장 조합이며 의도된 것이다.

**새 아이콘을 추가할 때는 path를 손으로 옮겨 적지 말 것** — 그게 위 문제를 만든 원인이다. `curl https://registry.npmjs.org/heroicons/-/heroicons-2.2.0.tgz`로 공식 패키지를 받아 `20/solid/<name>.svg` 또는 `24/outline/<name>.svg`의 내부 마크업을 통째로 복사하고, `class`(와 필요하면 `id`)만 남긴다. solid path의 `fill-rule="evenodd" clip-rule="evenodd"`를 빠뜨리면 구멍이 메워져 뭉갠 모양이 되므로 반드시 같이 옮긴다.

현재 매핑: 대시보드 `chart-bar` · 집품 현황 `cube` · 집품 리스트 `list-bullet` · 집품 할당 `squares-2x2` · 집품리스트 추출 `arrow-down-tray` · 트럭 현황 `truck` · 트럭 리스트 `table-cells` · 트럭 주기 `tag` · 계산기 `calculator`. 공용 버튼은 초기화 `trash` · 엑셀 업로드 `arrow-up-tray` · 출력 `printer` · 추가/생성 `plus` · 닫기 `x-mark` · 안내 `information-circle` · 층 패널 토글 `chevron-down` · 집품할당 카드 `user` · 주기 출력 `clipboard-document`. 대시보드 KPI 6개는 `inbox`/`clock`/`chevron-double-right`/`check-circle`/`arrow-up-right`/`clipboard-document-check`.

**예외 2가지**: `#pageLoadingOverlay`의 스피너(Tailwind 표준 `animate-spin` circle+path, Heroicons 아이콘이 아님)와 JsBarcode가 채우는 빈 `<svg>`들(`js/truck.js`의 `printBarcode-*`/`cycleBarcode-*`, `js/pick/gt-print.js`의 `.gt-label-barcode-svg`)은 이 컨벤션 대상이 아니다.

`#floorPanelToggleIcon`은 JS가 런타임에 `-rotate-90` 클래스를 토글하는데, Tailwind 브라우저 JIT는 런타임에 추가된 클래스의 CSS를 만들지 않아 실제 회전은 일어나지 않는다 — 아이콘 교체 이전부터 그랬던 기존 동작이니 아이콘 변경 회귀로 오해하지 말 것.

**6개 서브뷰 헤더의 아이콘 칩 + 그림자.** 사이드바 도입 전 옛 헤더가 쓰던 컨벤션을 그대로 복원했다(`git show <사이드바 도입 커밋 이전> -- index.html`로 확인) — 각 헤더(`#dashboardView`/`#homeView`/`#assignView`/`#extractView`/`#truckHomeView`/`#cycleView`)의 제목 좌측에 `<div class="bg-indigo-600 p-2 rounded-lg text-white shadow-md shadow-indigo-100"><svg class="w-6 h-6" .../></div>` 아이콘 칩을 두고, 제목/부제 블록과 함께 `flex items-center gap-3`로 감싼다. 아이콘은 해당 화면의 사이드바 서브내비 아이콘과 **같은 이름의 것**을 재사용하되, 크기가 `w-6`이므로 위 "아이콘 컨벤션"대로 24/outline 변형을 쓴다(사이드바 쪽은 20/solid). 헤더 바깥 바(`bg-white border-b border-slate-200 sticky top-0 z-20`) 자체에는 "헤더인 걸 알아보게" `shadow-sm`을 추가했다. `#truckHomeView`의 제목 태그도 이 기회에 다른 5개와 맞춰 `<h2 class="text-lg ...">`에서 `<h1 class="text-xl font-bold text-slate-900 tracking-tight">`로 통일했다(다른 코드에서 그 요소를 태그/크기로 특정해 참조하지 않아 안전).

**페이지별(서브뷰별) 흰 배경 헤더 — 전체 폭으로 꽉 채우는 구조.** `#pickApp`/`#truckApp` 내부의 "앱 레벨" 제목 헤더(집품 현황/트럭 현황 타이틀)는 사이드바 도입 당시 사이드바의 활성 항목이 이미 같은 이름을 보여준다는 이유로 제거됐었다(위 커밋 이력). 이후 새로 추가된 것은 그것과 다른 층위 — **6개 서브뷰 각각**(`#dashboardView`/`#homeView`/`#assignView`/`#extractView`/`#truckHomeView`/`#cycleView`, 사이드바 서브 항목과 1:1 대응)에 `bg-white border-b border-slate-200 sticky top-0 z-20` 헤더를 뒀다.

처음엔 각 헤더를 `max-w-[1600px] mx-auto px-8 py-8`로 이미 감싸인 뷰 콘텐츠 **안쪽**에 두고 `-mx-8 -mt-8 px-8`로 그 부모의 패딩만 상쇄하는 방식으로 구현했는데, 이 방식은 뷰포트가 `max-w-[1600px]`(+사이드바 폭)보다 넓을 때 헤더의 흰 배경이 그 max-width 박스 가장자리에서 멈춰버려("양끝이 딱 안 맞고 잘린 느낌") 실제 콘텐츠 컬럼 가장자리까지 못 채우는 문제가 있었다 — negative margin은 조상의 패딩만 상쇄할 뿐, 조상의 `max-width`/`mx-auto` 제약 자체를 벗어나지는 못하기 때문. 지금은 예전(사이드바 도입 이전) 헤더가 쓰던 "바깥은 전체 폭, 안쪽에서만 정렬" 패턴으로 재구성했다 — **각 헤더는 `max-w`/`mx-auto` 제약이 전혀 없는 순수 전체-폭 바(`bg-white border-b ... sticky top-0`)이고, 그 안에 제목/버튼을 정렬하는 `max-w-[1600px] mx-auto px-8 py-4` 내부 div가 하나 더 있다.** 뷰의 나머지 본문(테이블/카드 등)은 헤더와 별개로 자기 자신만의 `max-w-[1600px] mx-auto px-8 py-8` 래퍼로 감싼다 — 즉 각 서브뷰는 이제 "전체-폭 헤더 바 + max-w 제한 본문 래퍼" 2단 구조다. 예전에 여러 서브뷰가 공유하던 바깥쪽 `max-w-[1600px] mx-auto px-8 py-8` 래퍼(집품현황 3개 뷰 공통, 트럭현황 2개 뷰 공통)는 제거됐고, 각 서브뷰가 이제 자기 몫의 max-w 래퍼를 개별로 갖는다 — 새 서브뷰를 추가할 때 이 2단 구조(전체폭 헤더 + max-w 본문)를 그대로 따라야 한다.

**`#homeView`는 구조가 달라 특별 처리가 필요했다** — 원래 타이틀 행이 `div.space-y-3`라는 짧은 하위 래퍼 안에 있었고 `floorPanelCard`/`filterSortCard`/테이블은 그 래퍼의 형제(자식이 아님)였다. 헤더를 그 짧은 래퍼 안에 그대로 두고 `sticky`를 걸면 containing block이 그 짧은 래퍼가 되어, 스크롤 초반(래퍼 하단을 지나는 순간)에 헤더가 일찍 떨어져 나가 버린다(sticky는 자기 containing block 범위를 못 벗어남). 그래서 헤더를 `#homeView`의 직계 자식으로 끌어올렸다 — 새로 헤더/날짜탭 관련 UI를 만질 땐 이 구조를 유지할 것.

**"집품 할당" 버튼(`#assignOpenModalBtn`) — `#filterSortCard` 헤더에 묻혀 있어 위치가 애매하다는 피드백으로 전용 카드로 분리.** 원래는 `#filterSortCard`("필터·정렬" 카드)의 헤더 행에 제목과 나란히 작게 끼워져 있었는데, 지금은 `#filterSortCard`와 새 `#assignActionCard`를 감싸는 `grid grid-cols-1 lg:grid-cols-[1fr_14rem] gap-4` wrapper로 나뉘어 필터·정렬 카드 오른쪽에 버튼 전용 카드로 독립했다(`floorSectionsGrid`가 이미 쓰던 것과 같은 "좁은 화면 1열 스택 / 넓은 화면 나란히" 반응형 패턴). 처음엔 버튼이 `w-full h-full`로 카드 전체 높이를 채우도록 했었으나 "버튼이 너무 크다"는 후속 피드백으로, 지금은 카드 안에 위에서부터 사람 아이콘 칩(`bg-indigo-50 text-indigo-600 rounded-full`, Heroicons v2 `user` 단일 인물 — 그룹 아이콘이 "머리 두 개 붙은 것처럼 보인다"던 지적과 달리 여기는 사람 한 명이라 그 문제가 없다) + 설명 문구("선택한 데이터를 작업자에게 배정합니다") + 원래 크기의 작은 버튼, 이렇게 세로로 쌓은 `flex flex-col items-center justify-center text-center gap-2` 구조다. id(`assignOpenModalBtn`)와 클릭 핸들러(`js/pick/main.js`)는 그대로라 로직 변경은 없다 — 마크업 위치/스타일만 이동.

`#pickApp`/`#truckApp`은 각각 여러 모달을 자기 자신의 자식으로 품고 있다(`.hidden` 토글 시 모달도 같이 안 보이게 되긴 하지만, 모달이 열려 있을 땐 그 백드롭이 사이드바 클릭 자체를 막으므로 실무상 문제는 없다) — 서브뷰 구조를 리팩터링할 때 `</div>` 개수를 잘못 맞추면 이 모달들이 엉뚱한 위치로 새거나 앱 컨테이너가 모달보다 먼저 닫혀버리는 실수를 하기 쉬우니, 편집 후 반드시 `python3 -c "import re; ..."` 류의 div 밸런스 체크(단순 개수 비교가 아니라 스택 기반 push/pop으로 실제 중첩까지 확인) 또는 Playwright로 각 컨테이너의 `getBoundingClientRect()` 실측을 돌려볼 것.

**`filterQtySummary` pill 크기.** `js/pick/core.js`의 `renderFilterQtySummary()`가 만드는 4개 pill(필터 적용/전체/필터 N개/정렬 N개)은 원래 `text-[11px]`(고정 px, 루트 폰트 크기 확대의 영향을 안 받음)였는데 "너무 작아 안 보인다"는 피드백으로 `text-sm`(rem 기반, 루트 18px 기준 약 15.75px)+`px-3 py-1`로 키웠었으나, 이후 "조금만 줄여달라"는 후속 피드백으로 다시 `text-[13px]`(대괄호 고정 px, 이 코드베이스의 배지류 관례를 따름)+`px-2.5 py-0.5`로 살짝 축소했다 — 4개 pill(인디고/슬레이트/호박색/파랑) 전부 동일하게 적용.

**루트 폰트 크기.** `index.html`의 `<style>` 블록의 `html { font-size: ... }`은 기본값 16px → 18px(약 12.5% 확대) → 17px(현재값) 순으로 두 차례 조정됐다 — Tailwind 기본 타입/스페이싱 스케일이 전부 rem 기반이라(이 프로젝트는 커스텀 theme 설정이 전혀 없는 순정 Tailwind v4), 이 한 줄로 `text-xs`/`text-sm` 등 사이트 전반의 텍스트뿐 아니라 아이콘 크기·여백·사이드바 폭 등 전체 밀도가 함께 바뀐다(의도된 효과). 배지/캐럿용 `text-[9px]`/`text-[10px]`/`text-[11px]`/`text-[13px]` 같은 대괄호 px 고정값은 rem이 아니라서 이 설정의 영향을 받지 않으며, 의도적으로 그대로 뒀다(집품리스트 현황의 할당여부/필터요약 배지 등, 원래도 작게 유지하려던 UI).

## 인쇄(print) CSS — named `page` 속성과 페이지 나눔

`#printArea`(GT 라벨류)와 `#truckPrintArea`(트럭리스트 현황/트럭주기)는 CSS Paged Media의 `page` 속성으로 각각 이름 붙은 페이지(`pick-label`/`truck-a4`, 트럭주기 커스텀 텍스트 출력은 동적으로 `truck-cycle-note`)를 지정한다. **이 이름 붙은 페이지 지정은 반드시 `#pickApp`/`#truckApp` 앱 컨테이너 레벨에도 같이 걸려 있어야 한다** — `@media print` 블록에 `#pickApp { page: pick-label; }` / `#truckApp { page: truck-a4; }`가 그것이다. 이걸 인쇄 영역 자기 자신에만 걸어두면, 인쇄 시점에 내용이 비어 있어도 여전히 박스를 생성하는 형제 서브뷰들(다른 뷰들, `page: auto` 기본값)과 인쇄 영역(이름 붙은 페이지) 사이의 경계에서 브라우저가 강제로 페이지를 나눠버려 **1페이지가 빈 종이로 나오고 실제 내용이 2페이지부터 시작하는 버그**가 생긴다(named-vs-unnamed page 경계는 CSS Fragmentation에서 강제 페이지 나눔 트리거) — 앱 컨테이너에 같은 이름을 지정해두면 `page: auto`인 자식들이 조상의 지정을 그대로 물려받아 서브트리 전체가 처음부터 하나의 이름 붙은 페이지에 속하게 되어 이 경계 자체가 사라진다. `executeTruckCyclePrint()`처럼 동적으로 `page` 값을 바꾸는 흐름은 인쇄 영역과 앱 컨테이너 양쪽을 항상 같은 값으로 맞춰 설정/해제해야 한다(`js/truck.js`의 `executeTruckCyclePrint`/`finishTruckCyclePrint` 참고) — 한쪽만 바꾸면 다시 경계가 생겨 같은 버그가 재발한다.

GT 라벨류의 `.gt-label`(`js/pick/gt-print.js`의 `buildLabelHtml`/`buildBlankLabelHtml`)은 전부 Tailwind `break-after-page`가 붙어 있는데, 배치의 마지막 라벨에도 무조건 붙어 있어서 그대로 두면 인쇄 끝에 빈 페이지가 한 장 더 나온다 — `index.html`의 `@media print` 블록에 `#printArea .gt-label:last-child { break-after: auto !important; }`로 오버라이드해뒀다(ID+클래스+가상클래스 조합이 Tailwind 유틸리티 클래스보다 명시도가 항상 높아 로드 순서 걱정 없이 안전).

`.p-label-barcode svg`(트럭주기 라벨의 CT 바코드, `executeCyclePrint()`)에는 `max-width: 100%; height: auto;` 클램프가 있다 — JsBarcode는 인코딩할 문자열이 길수록 SVG 폭을 그대로 늘리므로(GT 라벨 쪽 `.p-barcode-stack-item svg`는 이미 이 클램프가 있었음), 이게 없으면 CT 코드가 길 때 A4 인쇄 영역 오른쪽으로 잘려 나간다. 새 라벨/바코드 CSS를 추가할 때는 이 패턴(폭이 데이터 길이에 좌우되는 SVG는 항상 `max-width:100%`)을 따를 것.

인쇄 관련 변경을 검증할 때 `window.print()`는 헤드리스 Chromium에서 실제 프린터 백엔드가 없어 `afterprint` 이벤트가 자연히 발생하지 않는다 — 대신 Playwright의 `page.pdf()`(CDP `Page.printToPDF`, `@page`/`page` CSS 규칙을 그대로 반영)로 실제 페이지네이션을 렌더링하고 `pymupdf`(`fitz`)로 페이지 수/텍스트를 읽어 확인하는 방법이 안정적이다(이 환경엔 `pypdf`가 시스템 `cryptography` 패키지와 충돌해 import 자체가 깨지므로 `pymupdf`를 쓴다). `printWithConfirm`의 확인-모달 게이팅 로직 자체를 테스트하려면 `window.print`를 몽키패치해 `afterprint`를 수동으로 dispatch하면 된다.

## `js/pick/*.js` structure (the files you'll touch most)

One file per domain, connected via `window.Pick` (see previous section). Grep the symbol name to find which file owns it rather than guessing from this table — it's a map, not gospel:

| File | ~Lines | Owns |
|---|---|---|
| `core.js` | 1250 | `COLUMNS`/`ALL_COLUMNS`, `state`, `els` (every `document.getElementById`, grabbed once), all localStorage keys + load/save helpers, generic utils (`escapeHtml`, `debounce`, `parseFlexibleDate`, `filterRowsExceptKey`...), zone-sort comparators (`compareZoneAscending`, `compareZoneWithOPriority`), `createSortBarController`/`createFilterBarController` factories + home's own instantiation (`homeSortBarController`/`homeFilterBarController`), home's filtering/sorting engine, floor panel, date tabs, `switchView` (now only `"home"`/`"assign"`/`"extract"` — no longer handles a `"dashboard"` case, since that view moved out to the top-level `#appSidebar`, see above), `assignConfigs` save/load |
| `gt-print.js` | 630 | GT 바코드 관리/매칭, 라벨 출력 (barcode HTML, print modals), 스페어(여분) 출력 |
| `assign-panel.js` | 1020 | 집품 할당 전체: `splitBalanced`/`rebalanceContiguousGroups` (the zone-balancing algorithm), `ASSIGN_DETAIL_COLUMNS`, `renderAssignPanel`/`renderAssignDetailTable`, `renderWorkerGroupCards` (container/groups/activeIdx를 인자로 받는 미리보기 카드+탭 렌더러 — `#assignCreateModal`의 `renderAssignPreview()`가 자기 상태를 넘겨 호출하는 얇은 래퍼일 뿐이고, `main.js`의 커스텀 할당 모달(`#customAssignModal`)도 자기 상태를 넘겨 그대로 재사용해 동일한 작업자별 카드+행 단위 상세 테이블을 보여줌), the assign screen's own filter/sort controllers (`assignFilterBarController`/`assignSortBarController`, built lazily — see gotcha below), create/confirm/delete-config flow, `getAssignedRowIdSet` (used by `core.js`/`home-table.js` for the "할당여부" column and by `main.js`'s home drag-select-assign to warn on re-assigning already-assigned rows) |
| `home-table.js` | 195 | Home table rendering + home's own drag-select/Ctrl-click multi-select |
| `extract.js` | 120 | 집품리스트 추출 screen: parses a 2-sheet excel (sheet1 in the standard home-upload format, reused via `matrixToRows`; sheet2 is a positional A-N layout with no header matching) and cross-references sheet2's H-column company set against sheet1's rows filtered by status="집품대기", renders a simple read-only preview table, and merges the result into home via the existing `applyParsedRows` |
| `dashboard.js` | 290 | 반출 대시보드(`#dashboardView`, 최상위 `#appSidebar`의 "대시보드" 탭 — `#dashboardApp` 컨테이너 안에 있으며 자체 서브내비 없음, 최초 진입 기본 화면): 자체 독립 데이터셋(`dashboardRows`, `state.rows`와 완전히 별개, localStorage 키 `pickListDashboardData`) — A~N(14열) 원본 엑셀/붙여넣기를 포지셔널로 파싱(A=그룹번호, C=내부반출번호, D=생성일시, H=업체명, N=상태값, J/K/L=수량 3단계), 6개 KPI 카드(할당대기/집품대기/집품중/상차준비완료/상차중/상차완료·반출완료) + Chart.js 도넛 2개(집품/상차 현황, 남은 수량·비율 계산) + 상태별 "OO 업체" 리스트 카드 4개(같은 업체가 서로 다른 그룹번호로 여러 건 걸려 있어도 구분되도록 (그룹번호, 업체명) 쌍 단위 dedup — 처음엔 업체명 H열만으로 dedup했으나 "그룹번호도 같이 보여달라"는 피드백으로 변경, 각 줄은 "그룹번호 · 업체명" 형식이고 그룹번호가 없는 예전 데이터는 구분자 없이 업체명만 표시, 카드 헤더의 "N개" 카운트도 고유 업체 수가 아니라 이 항목 수를 의미, 스크롤) 렌더링. 업로드는 **생성일자(D열) 단위 갱신** — 새로 올라온 데이터에 있는 날짜는 기존 `dashboardRows`에서 그 날짜분을 통째로 교체하고 다른 날짜는 보존한다(`mergeDashboardRows`, 더 이상 C열 dedup 아님 — 최신 스냅샷 유지가 목적). 날짜 탭에 `.date-tab-close`로 날짜별 삭제(`removeDashboardRowsByDate`, core.js의 `removeRowsByDate`와 동일 패턴) 가능. 하단 존별·층별 막대그래프(2열, `computeZoneQuantities`/`computeFloorQuantities`, 층별은 `getFloor`/`getFloorFamily` 재사용해 72/73→"7층"으로 합산)는 `state.rows`(집품리스트 현황 데이터)를 사용하되, 대시보드의 날짜 탭 선택(`dashboardActiveDateTabs`)에 맞춰 `getDateScopedStateRows()`로 스코프됨 — 서로 다른 데이터셋을 한 화면에서 같이 보여주는 유일한 지점이니 헷갈리지 말 것. 날짜 탭 구현 자체는 `renderDateTabs`와 별개의 자체 구현, `dashboardRows`의 생성일시 목록만 기준으로 탭을 만들지만 선택된 날짜 값은 `state.rows` 스코핑에도 그대로 재사용 |
| `main.js` | 545 | `refreshAll()` (re-renders whichever screen is visible) + **every** `addEventListener` call + the init sequence at the bottom (`loadFromStorage()`, `loadSortRules()`, `loadDashboardState()`, `loadAssignState()`, `loadGtState()`, first render) + `openCustomAssignModal`/`updateCustomAssignPreview`/`renderCustomAssignPreview`/`confirmCustomAssignment` (modal opened from home's drag-select "할당" button — lets the user pick headcount, default 1, before creating a `custom: true` assignConfig split via `splitBalanced`; preview reuses `assign-panel.js`'s `renderWorkerGroupCards` for the same per-worker card+table look as the official 집품 할당 modal, just without floor/date inputs). This is the last `<script>` loaded, once every other `js/pick/*.js` file has already populated `Pick`. |

**Both `createSortBarController`/`createFilterBarController` factories (in `core.js`) are reused verbatim by home and the assign panel** — before writing new filter/sort UI, check whether you can just instantiate these instead of hand-rolling HTML.

**집품 할당 행 드래그선택 + 드래그이동 (`createRowDragMoveController`, `core.js`).** 작업자 카드끼리 행을 옮길 때 매번 행마다 있는 `<select>`(여전히 존재, 특히 교차-config 이동에 필요)를 하나하나 고르는 대신, 여러 행을 드래그로 선택한 뒤 그 선택 덩어리를 다른 작업자 카드 위로 드래그해서 한번에 옮길 수 있다. 네이티브 HTML5 DnD가 아니라 홈 화면의 다중선택(`home-table.js`)과 같은 방식(수동 `mousedown`+`document mousemove`+`document mouseup`)이며, 이 앱 전체에 네이티브 `draggable`/`dataTransfer`는 파일 업로드 드롭존 2곳(`main.js`/`truck.js`, OS 파일 드롭 전용) 말고는 전혀 없다 — 새 드래그류 기능을 추가할 땐 이 관례를 따를 것.
- 팩토리는 마우스 이벤트 상태기계(6px 임계값으로 "새 범위선택 시작"과 "선택 뭉치를 들고 이동" 구분, 이동 중 커서를 따라다니는 "N개 이동중" 배지, `elementFromPoint` 기반 드롭대상 하이라이트)만 담당하고, 실제 배열 조작은 `onDrop(fromGroupKey, toGroupKey, rowIds)` 콜백에 전부 위임 — `Pick.createRowDragMoveController`로 export.
- 3곳에서 각자 독립 인스턴스로 씀: 집품 할당 페이지(`#assignView`, `assign-panel.js`의 `assignRowDragController` — `state.assignConfigs`를 직접 잘라붙이고 기존 `insertRowsSortedByZone` 재사용, `Pick.assignRowDragController`로 export해 `core.js`의 `switchView`가 다른 화면으로 이동할 때 `clearSelection()` 호출), 집품 할당 모달(`#assignCreateModal`, `assignPreviewRowDragController`)과 커스텀 할당 모달(`#customAssignModal`, `main.js`의 `customAssignRowDragController`) — 뒤 둘은 `renderWorkerGroupCards`가 5번째 인자로 받는 컨트롤러를 통해 in-memory 배열(`assignPreviewGroups`/`customAssignGroups`)을 `moveRowsBetweenGroups`(assign-panel.js, `Pick.moveRowsBetweenGroups`로 export)로 자른다.
- 선택 스코프는 `<tbody data-group-key>`로 작업자 하나에 한정(서로 다른 작업자 테이블에 걸쳐 선택되지 않음), 드롭 대상은 `.assign-drop-zone[data-drop-key]`(작업자 카드). 결과 화면(`#assignView`)의 행은 이미 지브라 줄무늬 배경 유틸이 붙어있어 선택 하이라이트에 단순 `bg-emerald-100` 토글을 쓰면 Tailwind 클래스 우선순위에 좌우돼 신뢰할 수 없으므로, `index.html`에 `!important` 전용 클래스(`.assign-drag-row-selected`)를 따로 두고 그쪽만 이걸 쓴다(미리보기 모달 둘은 지브라 줄무늬가 없어 기본값 `bg-emerald-100` 그대로 사용).
- 드래그앤드롭은 **화면에 실제로 렌더링된 작업자 카드끼리만** 가능하다(예: "작업자" 탭/필터로 카드가 숨겨져 있으면 그 작업자로는 드롭 불가, `#assignView`는 활성 config 하나의 카드만 렌더링하므로 교차-config 이동도 불가) — 이 경우는 기존 `<select>`로만 가능, 의도된 제약.
- Playwright로 검증할 때 `document.elementFromPoint` 기반이라 `page.mouse.move/down/up`(steps 지정)을 써야 하고, `locator.dragTo()`(네이티브 HTML5 DnD)는 이 코드 경로를 타지 않는다 — 또한 뷰포트 밖 좌표에서는 `elementFromPoint`가 `null`을 반환해 드롭이 조용히 무시되므로(에러 없이) 테스트 뷰포트를 대상 요소가 다 들어올 만큼 충분히 키워야 한다.

### Gotcha: `window.Pick` and reassigned bindings

Each file is `(function (Pick) { ... })(window.Pick = window.Pick || {})`. Anything meant to be used by another file must be assigned onto `Pick` (`Pick.foo = foo;`, usually grouped in an "exports" block at the bottom of the file); anything consumed from another file is read once via `var foo = Pick.foo;` near the top. `function` declarations and simple one-time `var` initializations (`state`, `els`, most constants/helpers) are safe with this "read once" pattern, because the underlying object/function reference never changes even if its *contents* are mutated later (e.g. `state.rows = ...`).

**The one thing this breaks: a binding that gets reassigned (`x = newValue`, not `x.prop = newValue`) after another file already read it.** A plain `var foo = Pick.foo;` only copies the value at that moment — it does **not** stay in sync with later reassignments the way an ES module's live `import` binding would. The current reassigned cross-file bindings, and how each is kept in sync:
- `assignFilterBarController` (`assign-panel.js`, read by `main.js`): built lazily inside `initAssignPanelControllers()`, called from `main.js`'s init sequence. The assignment site does `Pick.assignFilterBarController = assignFilterBarController = createFilterBarController(...)` (writes both the local var and `Pick` in one line), and `main.js` always reads `Pick.assignFilterBarController` directly rather than destructuring it at the top of the file.
- `homeMarkedIds` (`home-table.js`, read by `main.js`): reassigned in several places (`resetHomeMarkedIds()`, drag-select range application, `clearHomeSelection()`) — every reassignment site syncs `Pick.homeMarkedIds` in the same statement, and `main.js` reads `Pick.homeMarkedIds` directly instead of destructuring it.

If you add a new cross-file binding that gets reassigned (not just mutated), follow the same pattern: sync `Pick.name` at every reassignment site in the owning file, and never destructure that name in a consuming file — always read `Pick.name` fresh.

**Related but distinct gotcha: even a plain (non-reassigned) `Pick.foo` export only works if the owning file's `<script>` tag runs before the consuming file's.** `var getAssignedRowIdSet = Pick.getAssignedRowIdSet;` at the top of `home-table.js` captures whatever `Pick.getAssignedRowIdSet` is *at that instant* (when `home-table.js`'s script tag executes) — it is not a live binding, so if the owner were a file that loads later (e.g. `main.js`, which loads last), the capture would be permanently `undefined`. This is why `getAssignedRowIdSet` lives in `assign-panel.js` (loads before `home-table.js`) even though `main.js`'s own `homeSelectionAssignBtn` handler also calls it — `main.js` loading last is fine for *its own* `var getAssignedRowIdSet = Pick.getAssignedRowIdSet;` import, since by the time `main.js` runs, `assign-panel.js` has already set it.

Because function declarations are hoisted, code order *within* a single file mostly doesn't matter for correctness — but keep new code near its thematic section (and in the file that owns the state it touches) rather than appending to the end.

## Core state shapes

- `state.rows`: flat array of all uploaded picking rows (`{id, groupNo, deadline, createdAt, purchaseType, company, status, transportType, zone, quantity}`), persisted under `pickListData`.
- `state.assignConfigs`: array of picking-assignment configs, each `{id, floorInput, count, custom, customSeq, workerGroups, createdDates, printedWorkerIdx}`. `workerGroups[i]` is the array of raw rows assigned to worker `i`. `custom: true` configs (created via home's drag-select "할당" button → `#customAssignModal` → `confirmCustomAssignment` in `main.js`) have `floorInput: null` but, like regular configs, are split across `count` workers via `splitBalanced` (the modal lets the user pick headcount, default 1) — the only structural difference from a regular config is the missing floor/date filter, not the worker-group shape. `customSeq`("커스텀 1", "커스텀 2"...)를 발급하는 module-level counter `customAssignSeq`(`core.js`)는 `nextCustomAssignSeq()`로 증가만 가능하고, 홈 화면 탭/드롭다운의 커스텀 할당 라벨은 일반 config와 동일하게 `"커스텀 {customSeq} · {count}명"`처럼 인원수도 같이 보여준다(`assign-panel.js`의 `renderAssignTabs()`/`buildWorkerSelectOptions()` 두 곳 모두). `main.js`의 "전체 삭제"(`els.assignDeleteAllBtn`)는 `state.assignConfigs`를 비우는 것과 함께 `resetCustomAssignSeq()`(`core.js`, `customAssignSeq`를 0으로 되돌리고 `Pick.customAssignSeq`도 동기화)를 호출해야 한다 — 이게 없으면 전체 삭제 후 새로 만든 커스텀 할당의 번호가 0부터가 아니라 삭제 전 마지막 번호 다음부터 이어져 버린다(개별 config 삭제 시에는 번호를 재사용하지 않는 기존 동작 그대로 유지, 오직 "전체 삭제"만 리셋).
- Zone strings encode floor + sub-zone with no separator (e.g. `"72A"`, `"72O"` = floor 72, zone O) — `getFloor()`/`isOZone()` parse this. 72/73-floor zones whose name is `"O"` are physically first on the walking route, hence the "O존 우선 정렬" feature threaded through every sort comparator via a `zoneOPriority` boolean argument.
- Filter/sort UI state for a given screen is **not** persisted on the domain objects themselves (deliberately, to avoid saving throwaway UI state into `localStorage` alongside real data) — each screen keeps its own module-level map (e.g. `assignWorkerFilterState`/`assignWorkerSortState` keyed by config id) read through small getter helpers (`getAssignWorkerFilters(cfgId)`, etc.).
- `dashboard.js`'s `dashboardRows` (`{exportNo, createdAt, status, qtyTotal, qtyPicked, qtyLoaded}`, persisted under `pickListDashboardData`) is deliberately **not** on the shared `state` object — like `extract.js`'s `extractedRows`, it's a module-local var in the owning file, since no other `js/pick/*.js` file needs to read it.
- **Every modal open/close in the app locks/unlocks background page scroll** (`window.lockBodyScroll`/`window.unlockBodyScroll` in `js/shell.js`, reference-counted so nested modals don't unlock prematurely) — `js/pick/core.js`'s shared `openModalWithTransition`/`closeModalWithTransition` (covers 8 modals) call it, as do `js/truck.js`'s 3 hand-rolled modal pairs and `js/shell.js`'s own `showUiModal`. Every open/close function also has an idempotency guard (skip re-lock if already open, skip re-unlock if already closing) so a rapid double-click doesn't desync the counter. If you add a new modal, route it through `openModalWithTransition`/`closeModalWithTransition` rather than hand-rolling `hidden`-class toggling — otherwise its scroll won't be locked.
- **`lockBodyScroll` sets `overflow: hidden` on `document.documentElement` only, never on `document.body` too.** An earlier version set it on both simultaneously — this broke `position: sticky` for every sticky element on the page (`#appSidebar`, every sub-view's own header) in Chromium: with both ancestors' overflow non-`visible` at once, the sticky offset calculation collapses to 0, so the element renders at its plain static-flow position translated by the page's *actual, unchanged* scroll offset — i.e. it visually snaps up by exactly `-scrollY` px (looks like the sidebar/header "jump to the top" or vanish) the instant a modal opens while the page is scrolled down, and snaps back the instant it closes. `window.scrollY` itself never moves during this — confirmed live via `getBoundingClientRect()` diffing — so don't chase a scroll-position bug here, it's a sticky-rendering one. `documentElement`-only locking blocks real user scroll (mouse wheel/trackpad/keyboard) exactly as effectively as locking both (verified with `page.mouse.wheel()` in Playwright — note `window.scrollBy()`/`scrollTo()` are a bad test for this, since those JS APIs move `scrollY` regardless of `overflow: hidden` on *any* combination of html/body; they're not representative of real user scrolling). If you ever need to touch this function again, do not add `document.body.style.overflow` back.
- **Wide modals (`#assignCreateModal`/`#customAssignModal`, `max-w-6xl`) used to reserve `#appSidebar`'s width as left padding on their backdrop** (`pl-56`, matching `#appSidebar`'s own `w-56`) instead of the plain `p-4` every other modal backdrop uses — this made `flex items-center justify-center` center the modal within the content area to the right of the sidebar rather than the full viewport, avoiding the wide box's opaque left edge landing inside the sidebar at common laptop widths (1280–1440px). That fix was later reverted per explicit feedback that these two modals looked visibly off-center compared to every other modal on the page (confirmed via `getBoundingClientRect()`: with `pl-56` the box's center was offset ~110px right of true viewport center) — both now use plain `p-4` like every other modal backdrop, so they're dead-center consistent with the rest of the app, at the cost of reintroducing the original sidebar-overlap possibility at narrow viewport widths (accepted tradeoff, chosen by the user). Don't re-add `pl-56` to "fix" the overlap without checking whether the user still wants centering consistency instead.
- **Excel upload UI is now button→modal everywhere data gets uploaded** (대시보드/집품리스트 현황/트럭리스트 현황): a small header row (title+subtitle left, "데이터 로드됨" badge + "엑셀 업로드" button right) replaces what used to be an always-visible inline dropzone+paste card. The "데이터 로드됨" badge (`#pickActiveFileInfo` for 집품현황, `#activeFileInfo` for 트럭현황) used to live in the shared top `<header>` (visible app-wide); it's now relocated (same id, so JS toggle code is unchanged) into each page's own local header row — only pages with their own dataset (대시보드, 집품리스트 현황, 트럭리스트 현황) have one. 트럭현황의 사이드바 "홈" 항목(`#truckNavHomeBtn`, `#truckHomeView`)은 집품현황의 "집품리스트 현황" 개명과 동일한 패턴으로 "트럭리스트 현황"으로 라벨만 바뀌었다 — id/로직은 그대로. 집품 할당/집품리스트 추출(inline, unchanged)/트럭주기 deliberately do **not** get this treatment (no separate dataset, or explicitly left as-is per user decision). 트럭현황's upload modal (`#truckUploadModal`, opened via `openTruckUploadModal`/`closeTruckUploadModal` in `js/truck.js`) hand-rolls the same open/close transition idiom as its other modals (`openCycleGenModal`) since `js/truck.js` doesn't have access to `core.js`'s `openModalWithTransition`/`closeModalWithTransition`.
- **"집품리스트 현황"/"트럭리스트 현황" 라벨이 각각 "집품 리스트"/"트럭 리스트"로 재개명됨** (문구 단순화 피드백, "현황" 접미사 제거) — 사이드바 서브내비 라벨(`#navHomeBtn`/`#truckNavHomeBtn`), 각 서브뷰 헤더 `<h1>`(`#homeView`/`#truckHomeView`), 대시보드의 층별/존별 수량 카드 안내 문구, `js/truck.js`의 빈 상태 안내(`renderCycleDateTabs()`)와 토스트 메시지(`generateCycle()`류) 등 사용자 노출 텍스트 전부 동일하게 바꿨다 — id/로직/CLAUDE.md 위쪽의 다른 서술형 문단들(과거 개명 히스토리를 설명하는 문장 등)은 그 시점 기준 라벨명 그대로 두었으니, 현재 실제 화면 라벨은 이 항목을 최신 기준으로 볼 것.
- **트럭리스트 현황 수동 건 추가** (`#truckAddBtn`, 초기화 버튼과 엑셀 업로드 버튼 사이): `#truckAddModal`(`openTruckAddModal`/`closeTruckAddModal`/`confirmTruckAdd`, `js/truck.js`)이 `#truckUploadModal`과 동일한 hand-rolled 오픈/클로즈 트랜지션 idiom(`lockBodyScroll`/`unlockBodyScroll` + idempotency guard 포함)을 그대로 따른다. 그룹번호(선택)/생성일시(필수, `<input type="date">`라 `.value`가 이미 `YYYY-MM-DD`)/업체명(필수) 3개만 입력받아 `globalProcessedData[생성일시]` 배열에 row 하나(`{groupNo, company, picking:false, inputs:{palette:'KPP', emptyGt:0, palettePick:0, paletteGt:0}}`)를 push한다 — `processData()`(엑셀/붙여넣기 업로드 경로)와 동일한 `그룹번호_업체명` 키 중복 검사를 같은 날짜 버킷 내에서 재사용하고, 추가 후 `activeTabDate`를 그 날짜로 설정한 뒤 `renderDashboard(globalProcessedData)` 한 번만 호출해 탭/테이블/`localStorage` 저장까지 전부 갱신한다(트럭리스트 현황의 모든 데이터 변경 함수가 공유하는 단일 재렌더 진입점, 별도 patch 경로 없음).
- **트럭리스트 현황 "집품중" 토글 스위치** (`js/truck.js`의 트럭리스트 테이블 렌더링 부분): 체크박스였던 컨트롤을 스위치로 교체 — 이 앱 전체에서 첫 토글 스위치 UI라 새 컨벤션: 실제 `<input type="checkbox" class="sr-only peer">`는 시각적으로 숨기고, 뒤따르는 두 `<div>`(트랙: `w-9 h-5 bg-slate-200 rounded-full peer-checked:bg-amber-500`, 손잡이: `absolute ... w-4 h-4 bg-white rounded-full peer-checked:translate-x-4`)가 Tailwind의 `peer`/`peer-checked:` 변형으로 체크 상태를 반영한다. 셋 다 하나의 `<label>` 안에 있어야 클릭 시 실제 체크박스가 토글된다(라벨 전체가 클릭 영역). `onchange="toggleItemPicking(...)"` 배선과 `item.picking` 데이터 모델, 체크 시 행 전체에 `bg-amber-50/70` 하이라이트를 주는 로직은 이전 체크박스 버전 그대로 유지. **열 순서**는 두 차례 피드백을 거쳐 확정됨: 원래 맨 앞(그룹번호보다도 앞)이었다가, 업체명 셀 안에 나란히 넣었더니 "머리글을 따로 빼야 한다"는 피드백으로 별도 열로 되돌렸고, 다시 "업체명 머리글 옆에 위치"라는 요청으로 최종 순서는 **그룹번호 → 업체명 → 집품중(자기 헤더 유지) → 팔레트 종류 → ...** — 헤더 `<th>`와 각 행의 `<td>` 둘 다 이 순서로 옮겨야 함(하나만 옮기면 헤더/데이터 열이 어긋남).
- **집품리스트 현황의 "층별 인원 배치 계산" 카드 — 전체/필터 적용 수량이 각자 독립된 투입인원을 갖는다.** 원래는 카드 하나(`#laborInput`)를 두 섹션("전체 수량 (필터 미적용)"/"필터 적용 수량")이 공유해서, 인원수를 하나 바꾸면 양쪽 1인당 계산이 같이 바뀌었다 — 지금은 `#laborInputUnfiltered`/`#laborInputFiltered` 두 개의 독립 입력(각 섹션 헤더의 `<h3>` 옆에 위치, `localStorage` 키도 `LABOR_STORAGE_KEY`/`LABOR_STORAGE_KEY_FILTERED`로 분리)이라 서로 다른 인원수를 넣을 수 있다. **필터가 하나도 안 걸려 있으면 "필터 적용 수량" 카드(`#floorFilteredCard`) 자체를 통째로 숨긴다**(`hasActiveFilter()`로 판단, `core.js`에 이미 있던 함수 재사용) — 필터 미적용 상태에서는 필터 적용 rows와 전체 rows가 어차피 같은 집합이라 카드 두 개를 나란히 보여줄 의미가 없기 때문. 이때 `renderFloorPanel()`은 `floorTotalQty`/`floorPerPersonQty`/`floorBars`(필터 적용 카드 쪽 DOM)를 아예 갱신하지 않고 이전 값 그대로 남겨두며(카드가 숨어있어 안 보임), 감싸는 그리드(`#floorSectionsGrid`)도 `lg:grid-cols-2` 클래스를 필터 활성 여부에 따라 토글해 필터 미적용일 땐 전체 수량 카드가 한 칸(1열)만 차지하도록 한다. 접힌 상태 요약줄(`#floorPanelSummary`)은 필터가 걸려 있으면 필터 적용 결과 기준, 아니면 전체 수량 결과 기준으로 채워진다(필터 미적용일 땐 filteredResult 자체를 계산 안 하므로). **"투입인원" 라벨+입력창은 주변과 구분이 안 된다는 피드백으로 카드형 배경을 둘렀다** — `<label>`에 `bg-slate-50 border border-slate-200 rounded-lg px-2.5 py-1`을 추가하고, 그 안의 `<input>`은 라벨 배경과 겹쳐 보이지 않도록 `bg-slate-50`에서 `bg-white`로 바꿔 라벨(바깥 카드)과 입력창(안쪽 흰 박스)이 시각적으로 분리되게 했다 — 전체 수량/필터 적용 수량 두 섹션(`#laborInputUnfiltered`/`#laborInputFiltered`) 동일 적용.
- **트럭 주기 생성 모달(`#cycleGenModal`, `js/truck.js`) — 생성일시가 단일 select에서 Ctrl/Cmd+클릭 다중선택 탭으로 바뀜.** 원래 `<select id="cGenDate">` 하나로 한 번에 한 날짜만 고를 수 있었는데, 지금은 `#cGenDateTabsContainer`에 "전체"+날짜별 탭을 렌더링하는 `renderCGenDateTabs()`가 그 자리를 대신한다 — `js/pick/core.js`의 `renderDateTabs()`/`js/pick/dashboard.js`의 `renderDashboardDateTabs()`와 같은 "일반 클릭=그 날짜 하나만 선택, Ctrl/Cmd+클릭=다중선택에 토글" 관례를 세 번째로 복제한 것(CLAUDE.md에 명시된 대로 공유하지 않고 화면마다 자기 사본을 둠 — `js/truck.js`는 `window.Pick`과 무관한 별도 전역 스코프라 기존 팩토리를 못 가져다 씀). 선택 상태는 모듈 변수 `cGenActiveDateTabs`(빈 배열 = "전체")이고, `truckDashboardState_v1` 하나로 뭉쳐 저장하는 기존 `saveState()`/`loadState()` 객체에 필드 하나로 얹어 영속화(트럭 쪽은 집품현황과 달리 localStorage 키를 기능별로 나누지 않고 상태 전체를 객체 하나에 담는다).
  - 업체 리스트(`renderCycleGenCompanyList()`)가 여러 날짜를 동시에 보여줄 수 있게 되면서, 각 업체 행(`[data-company-row]`)이 자기 소속 날짜를 `data-date` 속성으로 들고 다녀야 한다(체크박스 `value`는 여전히 "그 날짜 배열 안에서의 인덱스"일 뿐이라 날짜 없이는 어느 원본 행인지 특정 불가) — `confirmGenerateCycle()`도 더 이상 날짜 하나가 아니라 각 행의 `data-date`를 읽어 그 날짜의 `truckCycleData`에 나눠 쌓는다. 같은 업체명이 다른 날짜에 중복 등장할 수 있어(처음엔 업체명 옆에 작은 회색 글씨 배지로 표시했으나, 아래 항목대로 지금은 자기만의 "생성일시" 열로 대체됨) 날짜 구분이 필요.
  - 업체 리스트 헤더 그리드(`grid-cols-[...]`, 7칸→8칸)에 "팔레트 종류"(`row.inputs.palette`, 트럭리스트 현황에 이미 있던 필드를 이 모달에서 처음 노출) 칸을 업체명 바로 뒤에 추가.
  - **후속 피드백으로 모달 폭/열 구성이 한 차례 더 조정됨**: `#cycleGenModalBox`가 `max-w-2xl`→`max-w-4xl`로 넓어져(업체명이 길 때 잘리지 않고 자연스럽게 줄바꿈되도록 — 업체명 `<span>`에서 `truncate` 클래스도 제거) 업체명 열이 충분한 폭을 확보했다. 업체명 옆 작은 날짜 배지는 없어지고, 대신 **"생성일시" 열이 테이블 맨 앞(체크박스 열보다도 앞)에 독립된 컬럼으로 추가**됐다 — 헤더 그리드(`index.html`)와 각 행 템플릿(`js/truck.js`의 `renderCycleGenCompanyList()`) 양쪽 다 `grid-cols-[5.5rem_1.25rem_1fr_5rem_4.5rem_4.5rem_5rem_5rem_5.5rem]`(날짜 열 `5.5rem`을 맨 앞에 추가, 팔레트종류 열은 빈GT와의 여백을 위해 `4.5rem`→`5rem`으로 소폭 확대)로 동일하게 맞춰야 한다 — 하나만 바꾸면 헤더/데이터 열이 어긋난다.
- **트럭 주기 페이지(`#cycleView`) 툴바 — 트럭 주기가 실제로 생성됐을 때만 나오는 버튼 줄이 따로 생김.** "초기화"/"생성"/"주기출력" 3개는 상시 노출 그대로 두고, "미사용 CT 자동매칭"/"출력"과 새로 추가한 "CT 바코드 초기화"/"순번초기화"/"순번 재계산" 5개를 `#cycleActionsToolbar`(테이블 바로 위, 상시 노출 줄 아래)로 옮겨 `truckCycleData`가 비어있으면 `hidden`, 하나라도 있으면 노출되게 함 — 토글은 `renderCycleTable()`(매 재렌더 시 `flatRows.length`로 판정) 및 `renderCycleDateTabs()`의 조기반환 분기(트럭리스트 현황 자체가 비어있는 경우) 양쪽에서 처리. "CT 바코드 초기화"/"순번초기화"/"순번 재계산"은 기존 `clearCtPool()`/`clearCycleData()`의 "확인모달 → 비우기 → 재렌더 → saveState → 토스트" 패턴을 따르되 전체 초기화가 아니라 특정 필드만 건드리는 narrow reset이다: CT 초기화는 `.ct`만 비우고 `releaseCtToPool()`로 pool에 반환, 순번초기화는 `.seq`만 빈 문자열로, 순번 재계산(`recalcCycleSeq()`)은 날짜+업체별로 묶어 생성 시점 규칙(`"{그룹크기}-{순서}"`)을 지금 남아있는 행 기준으로 다시 적용 — 행을 추가/삭제해 순번이 어긋났을 때 되맞추는 용도. **"출력" 버튼은 이 줄에서 맨 오른쪽에 위치**(다른 4개 버튼 뒤 DOM 순서 마지막 + `ml-auto`로 밀어냄) — 위쪽 상시 노출 줄의 "주기출력" 버튼이 이미 같은 `ml-auto` 관례를 쓰고 있어 그대로 맞춘 것.
- **"하얀 버튼"(흰 배경 ghost 스타일) 툴바/액션 버튼에 화면별로 겹치지 않는 배경색 부여.** "구분이 잘 안 된다"는 피드백으로, 각 화면에 상시 노출되는 기능 버튼들 — `#homeView`/`#assignView`의 `sortAddBtn`/`assignSortAddBtn`("+ 정렬 기준 추가", `bg-cyan-50`)과 `sortZoneOPriorityBtn`/`assignSortZoneOPriorityBtn`("72·73층 O존 우선 정렬", `bg-lime-50`, 동일 기능 버튼이라 두 화면에서 같은 색 사용), `#assignView` 작업자 카드의 3개 액션 버튼(`assign-panel.js`의 `renderAssignPanel()`, 이전엔 `outlineBtn` 변수 하나를 공유해 셋 다 흰색이었으나 지금은 `automatchBtnCls`(미사용 GT 자동매칭, `bg-violet-50`)/`gtResetBtnCls`(GT 바코드 초기화, `bg-fuchsia-50`)/`sparePrintBtnCls`(여분 출력, `bg-orange-50`) 3개로 분리), `#assignView` 상단 "GT 바코드 데이터 관리" 카드의 `gtPrintBtn`("GT 출력", `bg-blue-50`)/`customLabelBtn`("커스텀 라벨 출력", `bg-emerald-50`)/`labelMarginSettingsBtn`("라벨 여백 설정", `bg-yellow-50`), `#cycleView`의 `#cycleActionsToolbar` 4개 버튼(미사용 CT 자동매칭 `bg-violet-50`/CT 바코드 초기화 `bg-fuchsia-50`/순번 초기화 `bg-sky-50`/순번 재계산 `bg-amber-50`, 트럭현황 쪽은 같은 개념의 GT/CT 버튼과 색을 맞췄다) — 에 배경색을 입혔다. 색 선택 시 그 화면에서 이미 의미를 갖고 쓰이는 색(전역 공통: `indigo`=주요 액션, `rose`=초기화/삭제; `#assignView`만의 추가 제약: `WORKER_CARD_ACCENTS`(assign-panel.js:514-519)가 작업자 카드 식별용으로 `indigo`/`amber`/`teal`/`rose`/`sky` 5색을 이미 순환 사용 중이고, 여기에 `cyan`/`lime`/`violet`/`fuchsia`/`orange`(정렬·GT 액션 버튼)까지 더해 `#assignView` 한 화면에서만 이미 10개 hue가 쓰이고 있어 `blue`/`emerald`/`yellow`를 새로 골랐다)와 겹치지 않게 골랐다. **의도적으로 제외한 것**: 모달 안의 "취소" 버튼들과 날짜탭/필터칩/작업자탭 같은 활성-비활성 토글형 버튼(흰색=비활성 상태를 의미하는 기존 시각 언어라 색을 넣으면 활성/비활성 구분이 흐려짐), `homeSelectionAssignBtn`(플로팅 인디고 알약 안의 흰 "할당" 버튼 — 이미 인디고 배경 위에서 대비를 주기 위한 흰색이라 이번 케이스와 다름). 새로 흰 버튼을 추가할 때도 이 두 제외 규칙(모달 취소 버튼, 토글형 활성/비활성 버튼)은 그대로 흰색 유지가 맞다.
- **대시보드 "상차 현황" 도넛그래프의 전체/완료 수량 산정 범위가 확장됨.** `computeDashboardStats()`(`dashboard.js`)의 `loadTotal`(전체 수량)은 원래 "상차준비완료"/"상차중" 2개 상태의 K열(집품완료 수량) 합계였는데, 지금은 "상차완료"/"반출완료"까지 포함한 4개 상태의 K열 합계로 확장됐다. 완료 수량은 더 이상 `loadTotal - loadRemaining`으로 역산하지 않고, 새 필드 `loadCompleted`로 명시적으로 계산한다 — "상차중"/"상차완료"/"반출완료" 3개 상태(상차준비완료 제외)의 L열(반출완료 수량) 합계. `loadRemaining`(남은 상차 수량)은 그대로 "상차준비완료"/"상차중" 2개 상태의 (K-L) 합계로 변경 없음. 도넛 렌더링(`renderDashboard()`의 `renderDonutChart(..., stats.loadRemaining, stats.loadCompleted, ...)`)도 `loadTotal - loadRemaining` 대신 `stats.loadCompleted`를 직접 넘기도록 같이 수정했다 — 세 값(`loadTotal`/`loadRemaining`/`loadCompleted`)이 서로 다른 상태 조합·컬럼에서 독립적으로 계산되므로, 실제 데이터에서 "상차준비완료" 행의 L열이 0이고 "상차완료"/"반출완료" 행의 K열과 L열이 같다는 전제가 깨지면 `loadRemaining + loadCompleted`가 `loadTotal`과 정확히 일치하지 않을 수 있다(의도된 트레이드오프 — 세 값 각각의 정의를 요청대로 독립적으로 유지).
- **토스트 알림(`#toastContainer`, `js/shell.js`의 `window.showToast`)이 각 화면 헤더의 우측 버튼(초기화/엑셀 업로드 등)을 가려 클릭을 막던 문제 수정.** 토스트는 `fixed top-4 right-4 z-[110]`이었고, 헤더(`sticky top-0 z-20`)의 우측 버튼 클러스터도 뷰포트 우상단에 위치해 정확히 겹쳤다 — 개별 토스트 카드가 `pointer-events-auto`라(컨테이너 자체는 `pointer-events-none`) 토스트가 떠 있는 동안(페이드인 50ms + 표시 4000ms + 페이드아웃 300ms ≈ 4.35초) 그 자리의 클릭을 그대로 가로챘다. `top-4`→`top-24`로 내려 헤더 높이(약 72px) 아래에서 시작하도록 수정 — 헤더 우측 버튼과 겹치지 않으면서 여전히 화면 우상단에 눈에 띄게 표시된다.
- **`createRowDragMoveController`(core.js)에 드래그선택 요약 UI를 위한 `onSelectionChange(count)` 콜백 추가.** 원래 이 팩토리는 드래그선택 시 행 하이라이트(`selectedRowClass`)만 그리고, "이동중" 배지는 실제로 옮기는 carry 단계에서만 나왔다 — 순수 선택 단계에는 아무 요약 UI가 없었다. `paintSelection()` 끝에서 `options.onSelectionChange(markedIds.size)`를 호출하도록 추가해, 선택된 행 수가 바뀔 때마다(드래그선택/단일클릭/`clearSelection()` 전부 포함) 호출자가 자기 화면의 요약 바를 갱신할 수 있게 했다. 이를 이용해 3곳에 "선택 N행 + 선택 해제" 바를 새로 추가:
  - `#assignView` 자체 테이블(`assignRowDragController`, `assign-panel.js`): `#homeSelectionBar`와 같은 위치/스타일(`fixed bottom-6 left-1/2 -translate-x-1/2`)의 새 `#assignSelectionBar` — 단, 여기서는 "할당" 버튼이 필요 없어(이미 할당 화면 안이므로) 요약 문구 + "선택 해제"만 있다.
  - `#assignCreateModal` 미리보기(`assignPreviewRowDragController`)와 `#customAssignModal` 미리보기(`customAssignRowDragController`, `js/pick/main.js`): 각각 `#assignPreviewSelectionBar`/`#customAssignPreviewSelectionBar` — `#homeSelectionBar`처럼 페이지 레벨 `fixed` 플로팅 바가 아니라, **모달 자신의 마크업 안**, 스크롤되는 미리보기 영역과 확정/취소 버튼 사이에 있는 `shrink-0` div로 넣어 모달 밖으로 새지 않고 모달 하단에 고정된다. 모달을 닫거나(`closeAssignCreateModal`→`resetAssignCreateModal`, `closeCustomAssignModal`) 리셋할 때 해당 컨트롤러의 `clearSelection()`을 호출해 다음에 열 때 이전 선택/요약이 남아있지 않게 한다.
- **정식 집품 할당 생성(`#assignCreateModal`, 커스텀 할당 제외)은 홈 화면의 현재 정렬 설정과 무관하게 항상 고정 정렬로 나뉜다.** 원래 `getAssignRowItems`/`getAssignCompanyItems`(균등 할당/업체별 할당 두 모드 공용, `assign-panel.js`)가 `getSortedRows(getAssignBaseRows())`를 호출해 홈 화면의 `state.sortRules`/`state.zoneOPriority`를 그대로 반영했었는데, 사용자가 홈 화면 정렬을 바꾸면 배정 순서도 따라 바뀌는 게 의도와 다르다는 피드백으로 새 헬퍼 `getAssignFixedSortedRows()`(`getAssignBaseRows().slice().sort(...)`, `compareZoneWithOPriority` + 수량 내림차순 tiebreak 하드코딩)로 교체했다 — 존 오름차순(72·73층은 O존 우선) → 수량 내림차순이 항상 고정으로 적용되고, 홈 정렬 UI를 아무리 바꿔도 영향 없다. `getAssignBaseRows()`(필터만 반영, `core.js`)는 그대로라 **필터는 여전히 홈 화면에서 적용한 대로 반영**된다 — 이번 변경은 정렬만 고정했을 뿐 필터 로직은 손대지 않았다. 커스텀 할당(`main.js`의 `openCustomAssignModal`/`updateCustomAssignPreview`)은 `customAssignRows`(드래그선택 시점 스냅샷)를 직접 쓰는 완전히 별도 경로라 이 변경과 무관 — 여전히 드래그 시점의 홈 화면 정렬/필터를 그대로 반영한다.
- **업체별 할당의 물량 밸런싱은 "수량+업체수 가중합" 튜닝으로 개선하려 시도했으나 포기했다 (의도된 결정, 코드 변경 없음).** `splitBalanced`(assign-panel.js)에 플랫 가산 보너스와 ALPHA 블렌드 공식을 둘 다 실제 Playwright로 테스트해봤는데, "전형적인 점진적 분산 데이터에서 업체수 밸런스를 의미 있게 개선"하는 데 필요한 ALPHA 임계값과 "2개 업체가 물량을 지배하는 극단적 데이터에서 물량 밸런스가 파국적으로 깨지는" 임계값이 거의 같은 지점(~0.55–0.6)에서 교차해, 전역 파라미터 하나로는 두 목표를 동시에 만족시킬 방법이 없다는 걸 확인했다 — 실험 코드는 전부 되돌렸다(`git restore`). 대신 사용자가 상황(소량 다품목이면 균등 할당, 대량 소품목이면 업체별 할당)에 따라 수동으로 모드를 고르는 것으로 결론.
- **업체별 할당 균형 점수/그루핑 변경 히스토리 (재시도 방지용).** ① `splitBalanced`의 균형 점수를 "장수(행 개수)"→"서로 다른 존 개수"로 바꿔봤다가 되돌림 — 현재 `itemScore = it.qty + it.rows.length × (평균수량/장)`(장수 기준). **존개수 기준 밸런싱은 다시 시도하지 말 것**(사용자가 원래 장수 방식을 명시 선택). ② `getAssignCompanyItems` 그루핑 키에 층코드를 넣어 층 걸친 업체를 층별로 분리하는 건, 처음엔 "한 업체=한 사람 통째로" 취지를 깬다고 되돌렸으나, 이후 **"8층 입력 시 82/83이 안 섞이게(마지막 82 작업자만 83으로 크로스)" 요구로 다시 적용됐다** — 현재 `getAssignCompanyItems`는 `getFloor(zone) + "|" + company`로 그루핑한다(`item.company`는 라벨용이라 업체명 유지). **단일층 업체는 통째로 유지**(존 오름차순이라 원래도 층 안 섞이고 경계 1명만 크로스), **여러 층에 걸친 업체만** 이 키로 82파트/83파트로 갈려 그 업체가 83 행을 82 블록 중간에 끌고 오던 층 섞임이 사라진다(사용자 선택 — 층 분리 우선). 검증: 8층 3명, 단일층 업체는 변화 없음(경계 작업자만 82→83), 층 걸친 업체는 82/83으로 분리돼 작업자별 층전환 ≤ 1, Playwright 확인.
- **집품 할당(정식 생성, `#assignCreateModal`)은 2모드다 — 피킹 방식(한 업체씩 통에 담기 vs 집으며 분류)에 따라 고른다.** 두 모드 모두 `splitBalanced` 균등 + 층 분리(경계 1명만 크로스) + **층 경계 82O→83O 보존**을 공유하고, **아이템 소스와 배치(serpentine) 방식이 다르다**. 모달의 "할당 방식" 토글 버튼은 **`이동 최적 할당`이 왼쪽·기본 활성**, `업체별 할당`이 오른쪽이다(정적 마크업 활성 클래스는 `#assignModeBalancedBtn`에, `assignPreviewMode` 초기값도 `"balanced"` — 선언부와 `resetAssignCreateModal` 두 곳). 모달 상단의 안내 `<p>` 2개(층 코드 매칭 설명 / 두 모드 설명)는 사용자 요청으로 삭제됨 — "할당 방식" 라벨과 버튼만 남음.
  - **이동 최적 할당**(`assignPreviewMode==="balanced"`, **기본·왼쪽 버튼**): `getAssignRowItems`(행 하나=아이템, 업체로 안 묶음) + `applyFloorSerpentine`(존 오름차순 아이템을 층 블록으로 묶어 홀수번째 층 블록만 통째 뒤집어 82 올림/83 내림 → 경계 작업자 82O→83O). 존 순서로만 집어 이동 최소, 넓은 업체는 지나가며 쪼개 집어 **뒤로가기 0**. 카트에 업체별 칸 두고 집으며 분류하는 cluster 피킹용.
  - **업체별 할당**(`"company"`): `getAssignCompanyItems`(층-분리 `getFloor|company` 그루핑, 한 업체 통째) + **`applyCompanySerpentine`**(업체 단위 인접-최단 greedy). 한 업체를 통(박스) 하나에 다 담는 discrete 피킹용. `applyCompanySerpentine`은 각 업체를 "이전 업체가 끝난 존(rank)과 가까운 끝"에서 시작하도록 방향(오름=rows 그대로 / 내림=`rows.reverse()`)을 정한다 — 예: 앞 업체가 82O로 끝나면 다음 업체(82K·82O)를 **82O부터 내려오게** 뒤집어 업체 간 되돌아가기를 최소화. 층 경계는 보존: 층 블록을 홀짝으로 번갈아 진입 방향을 정하고(현행 serpentine과 동일 — 짝수 블록 로우 진입/홀수 블록 하이 진입), **다음 층 블록이 있는 블록의 마지막 업체는 그 층의 하이(존 rank 최대, 계단쪽)에서 끝나도록 강제**해 82O→83O 이음을 유지한다(그 대가로 마지막 업체 한 곳에 되돌아가기가 생길 수 있음, 사용자 선택: greedy + O존 경계 보존). 단일 층은 마지막 업체 강제가 없어 순수 greedy(넓은 업체는 여전히 통째라 내부 존은 오갈 수 있음). 존 거리 비교는 모든 아이템 존을 `compareZoneWithOPriority`로 정렬한 rank 인덱스 기반(72·73 O-우선도 자동 반영).

  모드 토글 인프라(`assignPreviewMode`/`setAssignPreviewMode`/`renderAssignModeButtons`/`ASSIGN_MODE_BTN_ACTIVE·INACTIVE`, 버튼 `assignModeCompanyBtn`/`assignModeBalancedBtn`)는 `index.html`/`core.js`/`main.js`/`assign-panel.js` 4곳에 걸쳐 있다. 정렬 기준 `getAssignFixedSortedRows`(고정 존 오름차순, c62b9b0). 72/73은 O존 우선(`compareZoneWithOPriority`, `/^7/`)이라 O가 맨 앞이라 경계 연결점이 최고 비-O존이 되지만 층 분리·serpentine 자체는 동일. 커스텀 할당(`#customAssignModal`, `main.js`)은 이 흐름 밖이라 무관.
  **히스토리(재시도 방지)**: per-company 지그재그(5eec811, 업체 경계 `72D→72C` 뒤로가기) → 지그재그 제거+`compareZoneWithOPriority` 정렬(업체 흩어짐) → 지그재그 복귀 → 업체 층별 분리(`getFloor|company`) → per-floor serpentine(82O→83O) → 행 단위+serpentine(넓은 업체 쪼개 뒤로가기 제거) → **2모드 통합**(둘이 거의 같아 토글 제거) → **다시 2모드 분리**(현장이 한 업체씩 통에 담는 discrete 피킹이라 업체 통째가 필요, 이동 최적은 cluster 피킹용으로 병존) → **이동 최적을 기본·왼쪽으로** + **업체별에 `applyCompanySerpentine`(인접-최단 greedy 역순) 도입**(업체별 모드에서 앞 업체가 82O로 끝나도 다음 업체가 82K부터 다시 시작해 되돌아가던 문제 해결, 다층은 O존 경계 보존). 완벽 균등 분배엔 LPT가 필요하나 작업자 담당 존이 흩어지는 대가로 미채택.
- **"업체 존 개수"(`companyZoneCount`) 파생 컬럼 추가 — 집품리스트 현황의 정렬/필터 UI.** 위 밸런싱 문제의 근본 원인이 "수량"이 아니라 "존에 얼마나 흩어져 있는가"라는 재정의에 따라, 기존 `groupCompanyTotal`("업체 총수량", `AGG_COLUMN`)과 완전히 동일한 파생 컬럼 배선 패턴을 그대로 복제해 추가했다 — 실제 row 필드가 아니라 렌더/필터 시점에 계산되는 값이다. `core.js`의 `ZONE_COUNT_COLUMN = { key: "companyZoneCount", label: "업체 존 개수", type: "number" }`을 `ALL_COLUMNS`에 추가하기만 하면(`homeSortBarController`/`homeFilterBarController` 둘 다 `columns: ALL_COLUMNS`를 받으므로) 정렬 추가 드롭다운/필터 버튼 목록에 자동으로 나타난다 — 추가 UI 배선 불필요. 계산은 새 함수 `computeCompanyZoneCounts(rows)`가 담당하며, `groupCompanyTotal`과 그루핑 기준이 다르다: `groupNo+company`로 수량을 합산하는 대신 **`getFloor(zone)+company`**(정확한 층 코드, `getFloorFamily`로 72/73을 "7층"으로 합치지 않음 — 사용자가 명시적으로 "층마다 따로"를 요청)를 키로 그 안의 **서로 다른 zone 값 개수**(`Set.size`)를 센다. 예: 한 업체가 72층에서 72A/72B/72C 3개 존, 73층에서 73A 1개 존에 걸쳐 있으면 72층 행들엔 3, 73층 행엔 1이 각각 표시된다(합쳐서 "7층 4개"가 아님). `getCandidateValues(key)`/`computeFilteredRows(baseRows)` 양쪽에 `groupCompanyTotal`과 나란히 동일 패턴(계산 → clone에 필드 추가 → `state.filters[ZONE_COUNT_COLUMN.key]`로 필터)으로 추가했고, `index.html`엔 `groupCompanyTotal` `<th>` 바로 뒤에 `<th data-key="companyZoneCount">` 추가(헤더 화살표는 `updateSortHeaderClasses()`가 `th[data-key]`를 제네릭 순회하므로 자동), `home-table.js`엔 같은 자리에 `<td>` 추가(단, `groupCompanyTotal`과 달리 `.toLocaleString("ko-KR")` 없이 `Number(...)`만 — 존 개수는 천단위 구분이 필요 없는 작은 정수라서). 이 범위는 집품리스트 현황(홈 화면)만이고, 집품 할당 화면(`assignFilterBarController`)에는 추가하지 않았다.
- **파생 컬럼(`groupCompanyTotal`/`companyZoneCount`)은 현재 걸린 필터를 반영해 cascade로 계산한다 (사용자 명시 선택).** `computeFilteredRows(baseRows)`/`getCandidateValues(key)`(둘 다 `core.js`) 모두 `gcMap`/`zcMap`(파생값 계산용 맵)을 각각 `preFiltered`/`exceptRegular`(다른 실제 컬럼 필터가 적용된 스코프)에서 계산한다 — 그래야 사용자가 "존=72" 필터를 걸면 테이블·드롭다운의 "업체 총수량"/"업체 존 개수"가 그 존 안에서의 값(예: 72A/72B에 걸친 업체는 총수량 8, 73층 포함 전체 18이 아님)을 보여준다. **이는 한 차례 반대로 갔다가 되돌아온 결정이다**: 처음엔 "파생 컬럼으로 먼저 필터를 걸고 다른 필터를 추가하면 행이 0건이 되는" 현상을 버그로 보고 파생값을 필터-이전 전체 스코프(`baseRows`/`getDateScopedRows()`)로 고정했었으나, 사용자가 원하는 건 정반대(값이 필터를 따라 바뀌는 cascade)임이 확인돼 철회했다. **그 0건 현상은 버그가 아니라 의도된 cascade 동작이다** — 예컨대 "업체 존 개수=3"을 먼저 걸고 "존=72A"를 추가하면, 72A 하나 안에서는 어떤 업체도 존이 3개가 아니므로(1개) 0건이 나오는 게 맞다("그 조합에 맞는 업체 없음"). 일반 컬럼끼리의 cascade(존 필터 → 수량/업체명 후보 좁혀짐)는 원래대로 정상. 새 파생 컬럼을 추가할 때도 이 cascade 스코프(`preFiltered`/`exceptRegular`)를 따를 것.

## Working with `js/pick/*.js`

Grep for the symbol name across `js/pick/` to find which file owns it (the table above tells you roughly what's where, but confirm with grep before editing — files grow). Each file is small enough (200–1250 lines) to `Read` in full once you know which one you need.
